# pnpm writes this file; reformatting it produces a diff of thousands of lines
# that says nothing about the dependencies.
pnpm-lock.yaml

# Build output, and a file Next regenerates on every build.
.next
next-env.d.ts

# The stylesheet is deliberately compact — one rule per line, comments aligned
# in a column — and so are its three siblings in the other samples, which it is
# a copy of. Prettier expands it to one property per line and nearly doubles
# it, which would leave the four sample stylesheets looking unrelated.
app/globals.css
