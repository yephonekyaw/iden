# The contents, not the directory: git never descends into an ignored
# directory, so a negation for a file inside one can never take effect.
.vscode/*
# Shared editor settings, which teach the YAML extension that MkDocs'
# `!!python/name:` tags are legitimate. Without it every clone reports
# mkdocs.yml as broken.
!.vscode/settings.json
claude/
.claude/
old-provider/
verification/
web/**/node_modules/
web/**/dist/

*.log
*.sql
*.sqlite

# Copied from the .example beside them and edited per deployment. deploy/.env
# holds the tunnel token, which is a credential.
.env
deploy/nginx/iden.conf

.DS_Store?
ehthumbs.db
Icon?
Thumbs.db
*.DS_Store

*.idea
*.iml

/web-app/WEB-INF
build/
*.[oa]
*.pyc

.hg
.svn
CVS

*~.nib
*.swp
*~

# MkDocs build output
site/
